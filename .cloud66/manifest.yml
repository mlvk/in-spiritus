staging:
    postgresql:
        server:
          same_as: rails
    redis:
        server:
          same_as: rails

production:
    postgresql:
        server: external
    redis:
        server: external
