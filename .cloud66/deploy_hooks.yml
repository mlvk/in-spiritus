production:
  after_symlink:
    - command: cd $STACK_PATH && sudo chown nginx:app_writers tmp
      target: rails
      run_on: all_servers
      sudo: true

staging:
  after_symlink:
    - command: cd $STACK_PATH && sudo chown nginx:app_writers tmp
      target: rails
      run_on: all_servers
      sudo: true
