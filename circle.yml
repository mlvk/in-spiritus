machine:
  services:
    - postgresql
    - docker

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker build -t $DOCKER_USER/in-spiritus:$CIRCLE_SHA1 .
      - docker push $DOCKER_USER/in-spiritus:$CIRCLE_SHA1
